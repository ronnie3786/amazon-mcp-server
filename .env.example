# Server Configuration
HOST=127.0.0.1
PORT=3000

# Authentication
# SECURITY: Generate a strong random token using: openssl rand -hex 32
# NEVER commit the actual .env file with your real token!
AUTH_TOKEN=your-secure-random-token-here-change-this
# Set this only for short-lived local testing without MCP auth.
ALLOW_UNAUTHENTICATED=false

# Amazon Configuration
AMAZON_DOMAIN=amazon.com
# Options: amazon.com, amazon.co.uk, amazon.de, amazon.ca, amazon.co.jp, etc.

# Browser Configuration
HEADLESS=false
# Set to 'false' for first-time login to see the browser and log into Amazon
# After logging in once, you can set to 'true' for headless operation
# Your session will persist in ./user-data/

# User Data Directory (for persistent browser sessions)
USER_DATA_DIR=./user-data
# This directory stores your Amazon login session (cookies, local storage, etc.)
# NEVER commit this directory - it contains sensitive authentication data!

# Session cookies are saved without extending Amazon's expiration by default.
# Set to true only if you explicitly want the older convenience behavior.
EXTEND_SESSION_COOKIES=false
